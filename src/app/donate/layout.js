export const metadata = {
  title: 'Donate · AshaJyothi',
  description: 'Sponsor a special-needs child or give once to purchase specific items (wheelchairs, daily meals, sanitary pads). Transparent 80G tax-exempt donations.',
};

export default function DonateLayout({ children }) {
  return children;
}
